{
  "name": "notebook-kit-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@observablehq/notebook-kit": "^1.5.0"
  },
  "scripts": {
    "docs:preview": "notebooks preview --root . --template template.html",
    "docs:build": "notebooks build --root . --template template.html -- index.html && mkdir -p .observable/dist/.observable && cp -r .observable/cache .observable/dist/.observable/"
  }
}
